{
  "name": "grpc-play",
  "version": "0.0.0",
  "dependencies": {
    "grpc": "^1.18.0"
  },
  "scripts": {
    "prebuild": "rm -rf build",
    "build": "tsc -p tsconfig.build.json --outDir build"
  },
  "devDependencies": {
    "typescript": "^3.3.1"
  }
}
